"""
Database configuration placeholder.

In this project, Django ORM with SQLite is used by default.
No manual database connection is required here.

This file exists to:
- Keep the project structure extensible
- Allow easy migration to PostgreSQL/MySQL later
- Match the assignment's expected layout
"""
